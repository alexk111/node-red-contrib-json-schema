{
  "bugs": {
    "url": "https://github.com/gorenje/node-red-contrib-json-schema/issues"
  },

  "bundleDependencies": false,
  "dependencies": {
    "ajv": "^8.12.0",
    "global": "^4.3.2",
    "@adobe/jsonschema2md": "^7.1.5"
  },

  "deprecated": false,
  "description": "JSON Schema validator for Node Red",
  "homepage": "https://github.com/gorenje/node-red-contrib-json-schema#README.md",
  "keywords": [
    "json",
    "node",
    "node-red",
    "schema"
  ],
  
  "license": "MIT",
  "main": "index.js",
  "name": "@gregoriusrippenstein/node-red-contrib-validation-and-documentation",
  "version": "0.0.7",
  "node-red": {
    "version": ">=3.0.0",
    "nodes": {
      "json-schema": "schema.js"
    }
  },

  "engines": {
     "node": ">=8"
  },

  "repository": {
    "type": "git",
    "url": "git+https://github.com/gorenje/node-red-contrib-json-schema"
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
